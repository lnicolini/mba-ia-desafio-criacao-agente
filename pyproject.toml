[project]
name = "residencial-aurora"
version = "0.1.0"
description = "Assistente virtual do Residencial Aurora (Google ADK)"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.141.1",
    "google-adk==2.9.1",
    "python-dotenv>=1.2.3",
    "uvicorn[standard]>=0.53.0",
]
